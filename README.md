# Databases
A cloud-based Student Management System developed using MySQL. It involved creating tables, inserting and retrieving records, and using INNER JOIN operations to manage relationships between entities. The project was deployed and accessed remotely by establishing connections through a secure shell (SSH).
